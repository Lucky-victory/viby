import { IdleDetectorDirective } from './idle-detector.directive';

describe('IdleDetectorDirective', () => {
  it('should create an instance', () => {
    const directive = new IdleDetectorDirective();
    expect(directive).toBeTruthy();
  });
});
